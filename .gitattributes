# 强制 LF —— 这不是风格偏好，是正确性问题。
#
# 本仓库的文件全部在 Linux 上执行。core.autocrlf=true 的 Windows 机器（默认安装的
# Git for Windows 就是）会把它们检出成 CRLF，之后：
#   - .sh   →  bash: $'\r': command not found（脚本第一行就死）
#   - .service → systemd 把 \r 当成值的一部分，ExecStart 路径解析失败
# 显式声明后，无论谁的 git 配置如何，仓库里和检出的都是 LF。

* text=auto eol=lf

*.sh      text eol=lf
*.service text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf
*.md      text eol=lf
LICENSE   text eol=lf
